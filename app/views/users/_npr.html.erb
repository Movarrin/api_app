`<div ng-controller="NprCtrl" class="npr-div" class="col-md-6">
      <h4 style="margin-left:30px;font-weight:bold;">NPR</h4>
    <ul ng-repeat="item in items"><li></li>
      <li>{{item.type}}</li>
      <li>{{item.title}}</li>
      <li>{{item.additionalInfo}}</li>

    </ul>
  </div>
